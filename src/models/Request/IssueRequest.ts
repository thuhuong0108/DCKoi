export interface IssueRequest {
  name: string;
  description: string;
  cause: string;
  reason: string;
  solution: string;
  issueImages: string;
  confirmImage: string;
  estimateAt: string;
  actualAt: string;
  issueTypeId: string;
  staffId: string;
}
