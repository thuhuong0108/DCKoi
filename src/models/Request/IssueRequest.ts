export interface IssueRequest {
  name: string;
  description: string;
  cause: string;
  reason: string;
  solution: string;
  issueImage: string;
  confirmImage: string;
  estimateAt: string;
  actualAt: string;
  issueTypeId: string;
  staffId: string;
}
